# iinpulib
 
Don't have enough time to clarify everything, but this is basically a library to write dynamic mixin-like functionality. (Ex. Method redirection annotation (runtime redirection), conditional class initializer/method executor)
